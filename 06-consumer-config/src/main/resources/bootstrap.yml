spring:
  cloud:
    config:
      # 指定configserver的地址
      uri: http://configserver.com:9999
      # 指定要访问的远程库的分支
      label: master
      # 指定要从远程库中读取的文件名，无需扩展名
      name: application-consumer-config
      # 指定要选择的环境
      profile: test